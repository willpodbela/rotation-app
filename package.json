{
  "name": "rotation-app",
  "private": true,
  "dependencies": {},
  "scripts": {
    "build": "cd client && npm install --legacy-peer-deps && npm run-script build && cd ..",
    "deploy": "cp -a client/build/. public/ && mv public/index.html public/react.html",
    "postinstall": "npm run-script build && npm run-script deploy && echo 'Client built!'"
  }
}
